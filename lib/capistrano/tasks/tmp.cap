namespace :deploy do
  namespace :tmp do
    desc 'Removes all tempfiles'
    task clear: [:set_rails_env] do
      on roles(:app) do |host|
        within release_path do
          with rails_env: fetch(:rails_env) do
            execute :rake, 'tmp:clear'
          end
        end
      end
    end
  end
end
