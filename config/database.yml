defaults: &defaults
  adapter: postgresql
  encoding: utf8
  min_messages: WARNING

development:
  <<: *defaults
  username: pgadmin
  password: devel
  host: localhost
  database: gtgraphics_development

test:
  <<: *defaults
  username: pgadmin
  password: devel
  host: localhost
  database: gtgraphics_test

staging:
  <<: *defaults

production:
  <<: *defaults