!!!
%html{lang: I18n.locale, class: 'no-js', prefix: 'og: http://ogp.me/ns#'}

  %head
    %title
      - if content_for? :title
        = yield :title
      - else
        = ([translate('views.title')] + breadcrumbs.collect(&:caption).drop(1)).reverse.join(' &middot; ').html_safe

    / Meta Elements
    %meta{name: 'viewport', content: 'width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no'}
    = csrf_meta_tags
    %meta{property: 'og:site_name', content: 'GTGRAPHICS'}
    %meta{property: 'og:type', content: 'website'}
    = facebook_meta_tags
    = yield :head

    / Stylesheets
    = yield :stylesheets

    / Favicons
    %link{rel: 'shortcut icon', href: path_to_image('favicon.png')}
    %link{rel: 'apple-touch-icon', href: path_to_image('apple-touch-icon-precomposed.png')}
    / JavaScripts
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
    = i18n_javascript_file
    = yield :javascripts

  %body{id: yield(:body_id).presence, class: body_css, style: body_styles, data: { namespace: controller_namespace, controller: controller_name, action: action_name, locale: I18n.locale }}
    = i18n_javascript

    - if content_for? :content
      = yield :content
    - else
      = yield
