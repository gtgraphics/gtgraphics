= simple_form_for [:admin, @image], html: { class: 'form-horizontal' } do |form|

  = form.simple_fields_for :translations do |translation_fields|
    = render 'translation_fields', fields: translation_fields

  = form.input :asset, as: :image_preview, required: true, input_html: { preview_version: :thumbnail }, label_html: { class: 'col-lg-2' }, container_html: { class: 'col-lg-8' }
  = form.association :author, label_html: { class: 'col-lg-2' }, container_html: { class: 'col-lg-8' }

  .form-group
    .col-lg-10.col-lg-offset-2
      = form.button :submit, class: 'btn-primary'
      = button_link_to translate('helpers.links.cancel'), [:admin, @image]