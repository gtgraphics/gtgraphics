- output = nil

- simple_form_for [:admin, @attachment], url: '' do |form|
  - form.simple_fields_for :translations, child_index: microtimestamp do |translation_fields|
    - output = render 'admin/attachments/translation_fields', fields: translation_fields

= output