= index_headline_for Attachment do
  .btn-toolbar
    = create_button_link_for Attachment, icon: :plus, caption: translate('helpers.links.upload', model: Attachment.model_name.human), url: :new_admin_attachment

- if @attachments.any?
  = render 'table', checkboxes: true
- else
  .well.text-center
    %h4= translate('views.hints.empty', model: Attachment.model_name.human(count: 2))