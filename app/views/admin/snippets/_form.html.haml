= simple_form_for [:admin, @snippet], html: { class: 'form-horizontal' } do |form|

  = toolbar do
    .btn-group
      = form.button :submit, class: 'btn-primary'
 
  = translation_manager_for form, url: translation_fields_admin_snippets_path