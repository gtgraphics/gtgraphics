.page-header
  %h1= page.title

= render_region :main

- if @content_pages.any?
  %ul
    - @content_pages.each do |content_page|
      %li= link_to content_page, content_page

= render_region :footer